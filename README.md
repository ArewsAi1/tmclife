# TMC Life Cloudflare Site

Source-controlled deployment for tmclife.net.

The Cloudflare build mirrors the current stable Pages deployment and then applies files from `overrides/`. Future site fixes should be committed as individual files inside `overrides/`.
